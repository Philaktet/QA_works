{
  "name": "cypress_fresh",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cypress open",
    "test": "node -e \"const cp = require('child_process'); cp.exec('cypress run --quiet', (err, stdout) => { console.log(stdout.match(/error|fail|✖/) ? stdout.match(/(error|fail|✖).*/gi).join('\\n') : 'All tests passed'); })\""
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "cypress": "^14.5.2"
  }
}
