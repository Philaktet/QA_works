{
  "name": "cypress_fresh",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cypress open",
    "test": "cross-env-shell \"cypress run --quiet | grep -E '(error|fail|✖)' || echo 'All tests passed'\""
  },
  "devDependencies": {
    "cypress": "^14.5.2"
  }
}
